// Write a program to Calculate the area of a rectangle.
#include<stdio.h>
int main (){
    int length, width;
    scanf("%d %d",&length,&width);
    printf("Area of a rectangle = %d",length*width);
return 0;
}