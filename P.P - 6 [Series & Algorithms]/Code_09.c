// 9 -Write a program to print the ASCII code of ‘s’ and ‘S’.(Input must be taken from the user.)
#include<stdio.h>
int main () { 
    char ch; 
    scanf("%s",&ch);
    int ASCII_code_number = ch; 
    printf("%d",ASCII_code_number);
    return 0; 
}