// Implement a program that takes a boolean input (0 or 1) and prints the opposite value using the logical NOT operator.
#include<stdio.h>
int main () { 
    int number; 
    scanf("%d",&number);
    printf("%d",!number);
}